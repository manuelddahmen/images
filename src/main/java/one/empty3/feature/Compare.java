package one.empty3.feature ;
/*
import one.empty3.io.ProcessFile ;
import java.io.File ;
public class Compare extends ProcessFile {
     public boolean process (File in, File out ){
        PixM p = PixM.getPixM() ;
PixM p2 = PixM.getPixM() ;
new DiffE().dist(p, p2)
} 
} 
*/
